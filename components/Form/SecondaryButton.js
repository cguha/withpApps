import React from 'react';
import { Button } from 'react-native-elements';
import styles from './styles';

const SecondaryButton = (props) => {
  return (
    <Button buttonStyle={styles.secondaryButton} />
  );
};

export default SecondaryButton;
